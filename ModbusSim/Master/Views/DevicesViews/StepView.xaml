<Window x:Class="Master.Views.DevicesViews.StepView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Master.Views.DevicesViews"
        mc:Ignorable="d"
        Title="StepView" Height="450" Width="800">
    <Window.Resources>
        

        <Style TargetType="DataGridRow" x:Name="BaseStyle">
            <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick" />
        </Style>
    </Window.Resources>

    <Grid>
        <DataGrid ItemsSource="{Binding Processes}" SelectionMode="Single" AutoGenerateColumns="False"
				  CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False"
				  SelectionUnit="FullRow"
				  Name="dgSteps">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Process Type" Binding="{Binding ProcessType}" IsReadOnly="True" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
